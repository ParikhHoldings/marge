# Active Goal Audit

Last updated: 2026-05-17

This audit maps the active user goal to concrete evidence in the repository. It is intentionally stricter than "tests pass": local smoke coverage proves the MVP behavior, but a pilot pastor is not ready until production configuration and at least one live church-tool connector are verified.

## Restated Success Criteria

The active goal is achieved only when Marge can do all of the following for a first pastor signup:

1. Preserve the research and project vision for future LLM/Codex work.
2. Stop feeling like a static demo by keeping real workspaces separate from demo mode.
3. Make chat useful for saving and retrieving ministry context, not just returning canned replies.
4. Ask ministry-context questions that make Marge specific to the pastor and church.
5. Connect to church tools securely, with encrypted credentials and safe verification before sync.
6. Be proactive through a desk, approval queue, first-week plan, and reviewable drafts.
7. Keep the pastor in control before any external send or writeback.
8. Survive production readiness checks with real deployment config and at least one live external provider.

## Prompt-To-Artifact Checklist

| Requirement | Evidence | Current status |
| --- | --- | --- |
| Save the research | `docs/PASTOR_RESEARCH.md`, `docs/PASTORAL_SECRETARY_UX.md`, `docs/AI_AGENT_ROADMAP.md`, `AGENTS.md` | Implemented. These files preserve pastor pain research, product direction, agent roadmap, and repo-specific LLM guidance. |
| Give LLMs project understanding | `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `.github/copilot-instructions.md`, `BUILD_SUMMARY.md`, `docs/LAUNCH_PLAN.md`, `codex-task.md` | Implemented. `AGENTS.md` is canonical, the bridge docs point agents to it, stale launch/build docs now describe the current state, and the old task prompt is marked historical. |
| Reduce placeholder/demo behavior | Account-scoped live mode, first-record setup steps, demo/live gating in `/assistant/desk` and `/briefing/today` | Locally verified by `scripts/smoke_first_run.py` and browser checks. |
| Make first signup personal | First-run profile, contextual interview question, personal support-style question, Assistant hero copy, contextual setup card, onboarding panel, suggested prompts | Locally verified by browser snapshot on 2026-05-17: first Assistant screen showed "Let's learn Header Smoke Church..." and asked the role question. `scripts/smoke_first_run.py` also asserts the first desk question and first setup card name the pastor's church, and now verifies Marge asks and saves how the pastor wants to be supported personally. |
| Chat saves ministry context | `POST /assistant/chat`, account profile updates, persisted chat history | Locally verified by `scripts/smoke_first_run.py`. |
| Chat retrieves ministry memory | Person, visitor, care, prayer, connected-context lookup paths in `app/routers/assistant.py` | Locally verified by `scripts/smoke_first_run.py` and `scripts/smoke_connected_providers.py`. |
| Natural-language writes are safe | Visitor logging, single-name handling, no-name rejection, care/prayer/member note writes, queued action fallback | Locally verified by `scripts/smoke_first_run.py`. |
| Visitor follow-up is reviewable | `app/services/visitor_followup.py` and `AssistantAction` email drafts | Locally verified by `scripts/smoke_first_run.py` and `scripts/smoke_mcp_first_run.py`. |
| Drafts are server-backed, not browser placeholders | `POST /drafts/`, live detail panel draft flow, `app/services/marge.py` draft helpers, backend-prepared `email_draft` actions | Locally verified by browser check and smoke assertions that "Pastor Pastor" is not generated and prepared care actions use the server draft template instead of old browser-only wording. |
| Tool connections are secure | OAuth/API-key setup, encrypted `IntegrationCredential`, credential-scoped `verified_at`, safe verify endpoints, chat/MCP credential checks, sync rejection before verification, disconnect, metadata-only audit | Locally verified with mocked/local providers by `scripts/smoke_integrations.py` and `scripts/smoke_connected_providers.py`. |
| External writes require approval | `AssistantAction` approval queue, pastor/admin/owner-only action endpoints, approve/execute flow, and provider policy gates | Locally verified by `scripts/smoke_integrations.py` and `scripts/smoke_connected_providers.py`. |
| Pastoral data is role-gated | Pastor/admin/owner gates for briefing, drafts, member detail/notes, care, prayer, member/visitor writes, assistant actions, chat history, audit, connected context, and sync | Locally verified by `scripts/smoke_integrations.py`; staff tokens can read the live desk and basic member/visitor directories but cannot inspect approvals or mutate pastoral records. |
| MCP clients can use Marge | `mcp_server/server.py`, `mcp_server/README.md`, MCP first-run tools | Locally verified by `scripts/smoke_mcp_first_run.py`. |
| Production schema is managed | Alembic migrations and `scripts/verify_migrations.py` | Verified in `scripts/verify_pilot_readiness.py`: migration schema passed. |
| Production config is safe | `.env.production.example`, `app/runtime_config.py`, `scripts/verify_production_config.py` | Not complete in the local environment. The readiness gate fails until deployment secrets are provided, and production runtime enforcement now fails closed when guardrails are missing. |
| Public deployment bootstraps first-run app | `scripts/verify_deployment_bootstrap.py`, `/`, `/health`, `/assistant/config`, `/app` | Implemented. Local development can pass with `--allow-relaxed-account-tokens`; production/pilot readiness requires runtime strict workspace-token mode. The verifier now also checks that public `/` and `/assistant/config` expose only generic first-run/bootstrap data. |
| First-run workspace journey can be rehearsed | `scripts/verify_first_run_workspace.py` | Implemented and locally verified. The script creates a disposable workspace, exchanges a session token, completes onboarding through chat, verifies first-record setup, logs a visitor through chat, queues a welcome draft, and confirms visitor follow-up lookup recalls the saved visitor. Remote runs require `--allow-remote-write`. |
| At least one live external provider is verified | `docs/PILOT_CONNECTOR_RUNBOOK.md`, `MARGE_ACCOUNT_TOKEN=... scripts/verify_live_integrations.py --include-mcp --require-live-provider` | Not complete. The runbook now gives the operator path for choosing the first provider from the pastor's ministry burden, running the no-sync credential check, saving evidence, and stopping before unsafe sync/writeback; no Google Workspace, Microsoft 365, Planning Center, Breeze, or Rock RMS credential has been verified in a real workspace yet. |

## Verification Run

Most recent local verification completed on 2026-05-17:

- Post-email-validation continuation: passed `.venv/bin/python scripts/smoke_first_run.py`, `.venv/bin/python scripts/smoke_mcp_first_run.py`, `.venv/bin/python -m compileall app mcp_server scripts`, SQLite smoke residue check (`0`), SQLite `PRAGMA foreign_key_check` / `PRAGMA integrity_check` (`ok`), and `git diff --check`. Uvicorn was stopped after server-backed smokes.
- Off-script first-run fallback: passed. `scripts/smoke_first_run.py` now verifies a complete but still-empty first-run workspace keeps unmatched chat like "Can you be my secretary?" anchored to the active first-record setup card and setup-aware prompt chips instead of answering as if the desk is clear. After the first real visitor is saved, the same unmatched prompt leads with the real visitor card before connector setup.
- Workspace-first connector guard: passed. `scripts/smoke_integrations.py` now verifies connector setup and legacy Rock sync still reject missing workspace context even when local legacy token mode is relaxed, so OAuth state, API-key credentials, synced context, and connector writes cannot land in unscoped legacy rows.
- `.venv/bin/python scripts/smoke_first_run.py`: passed.
- Signup specificity: passed. The first-run smoke now asserts `/assistant/signup` rejects a missing church name instead of creating a generic `New Church` workspace.
- Signup recovery identity: passed. The first-run smoke now asserts `/assistant/signup` rejects missing or invalid owner email addresses, while the visible workspace form requires pastor name, email, and church name before creating the first workspace.
- First-run chat specificity: passed. The smoke now uses natural pastor wording for role, attendance, church context, church voice/tradition, first ministry priority, personal support style, tools, follow-up pain, weekly rhythm, and approval guardrails; it also verifies command-like onboarding answers such as "Help me close loops...", "Nudge me gently...", "Connect Planning Center and Gmail", and "Write in a warm and brief tone" save the intended profile fields instead of being skipped as commands. It verifies terse answers such as "Solo.", "72.", "Nudge me gently.", "Planning Center and Gmail", "Warm, brief, pastoral", and "Baptist roots; avoid insider language" normalize into the right profile fields, and that "Pastor Ben." saves as `Pastor Ben` when Marge is asking for the pastor name. Generic plural guest language is not logged as a fake visitor. It also verifies "How will you use this context?" explains how saved context shapes priorities, drafts, secure setup, and approvals; "How do secure connections work?" explains no-passwords-in-chat, no-sync credential checks, and approval boundaries while attaching connector setup cards; "Why is this the next step?" explains the active setup step and keeps the first-record card attached; "Help me add a ministry update" guides concrete pastoral logging and points back to the first real record while `data_seed` is pending; "What should I do for first-time guests and private prayer needs?" answers from saved ministry priority context and the active first-record setup; named-church recap prompts such as "What do you know about First Run Smoke Church..." summarize saved ministry context instead of failing as a person lookup; saved-person, person-context, prayer-context, care-context, absence-context, and follow-up-draft suggested prompts carry the real person name forward instead of unresolved "them"/"Log a contact" prompts; unknown named-person prompts carry the name forward into a safe add-person suggestion and generic "Add this person first." returns capture guidance instead of creating a placeholder; request-level private prayer prompts do not label unnamed requests as `Pastor`, do not create fake people, can answer "What should I capture for private prayer?", can queue a reviewable draft from the private request, and generic "Add a prayer request." prompts do not create empty prayer records; generic "Add a care case." and "What should I capture for a care case?" prompts explain person/category/latest-contact requirements and do not create empty care cases; "Who has been absent?" answers from live attendance/absence desk items and attaches absence follow-up actions; "Log that I visited Janet Ellis today" saves a pastoral contact from a named prompt; "Where can I fit a visit with Janet Ellis?" queues a reviewable calendar block using saved care context, weekly rhythm, and approval/writeback boundaries; "Draft a scheduling reply" creates a focused reviewable scheduling draft from saved weekly rhythm and ministry calendar context instead of generic reply drafts; "What calendar details do you need?" explains title/date/start-time requirements, Google Workspace or Microsoft 365 setup/check cards, and approval/writeback boundaries without creating approval actions; "What can wait until next week?" triages real people/review items, explains deferrable work, and keeps approval boundaries visible; "What should I handle next?" returns concrete next work with cards instead of generic chat; "What should I review first?" and "What should I approve first?" summarize pending review work without mutating action status; "Open integrations" returns actionable saved-tool setup cards and explicitly excludes MCP from church-tool readiness; "Sync the connected tools" runs a credential-aware precheck, excludes MCP as a church tool, attaches saved connector setup cards, and does not default to syncing Google before verification; "show my setup steps" returns data-seed and connector cards; "what should I ask a new family?" returns first-record coaching with the visitor setup card; prayer-focused profiles get `Add the first real prayer request` plus private-prayer coaching and retire that setup action after a concrete prayer save; care/hospital/grief-focused profiles get `Add the first person needing care` with a person-first setup form and can create the person plus care case from a named chat prompt; "prepare my first-week plan" returns the reviewable first-week plan action with visitor/tool setup; "how can you help me this week?" routes through the saved ministry operating plan with the visitor setup card; and "what can you do before tools are connected?" plus "explain the approval rules" return concrete, non-placeholder guidance.
- First-run approval empty states: tightened. Empty approval/review questions and approval-rule prompts now stay setup-aware while no real review work exists, attach the first-real-record card, and do not suggest canned draft/today-queue work. Named approval commands such as "Approve the welcome draft" or "Approve the Marcus welcome draft" no longer fall back to approving unrelated setup actions or another person's pending draft when no matching draft exists.
- Connector language during onboarding: passed. `scripts/smoke_first_run.py` now verifies a natural tools answer like "Planning Center for kids check-in and Gmail" is saved as `Planning Center, Gmail` instead of being misrouted to connector credential verification. The credential-check detector now ignores check-in/check-ins language while preserving explicit credentials, connection, verify, test, and health-check commands.
- Live reply wording cleanup: passed. `scripts/smoke_first_run.py` now asserts setup-step reasons, empty live morning briefings, and person-capture guidance do not describe the pastor's real workspace with prototype/placeholder wording while still explaining first real records, secure setup, and approval boundaries.
- Support-style prompt routing: passed. `scripts/smoke_first_run.py` now verifies "How will you support me?" has a dedicated `support_style_guidance` chat path. Before the support preference is saved, Marge asks the contextual support question and attaches the `setup-profile-support_preferences` card instead of saving the question itself as the answer. After the preference is saved, Marge answers from the pastor's support style and keeps approval/credential boundaries visible.
- Proactive support style: passed. `scripts/smoke_first_run.py` now verifies the completed first-run desk `proactive_summary` carries the pastor's saved support preference, including "Nudge me gently" and "protect my rest", while still naming the concrete first-record setup step.
- Proactive first-run context: passed. The completed first-run desk `proactive_summary` now also explains the next first-record setup step from the pastor's saved follow-up pain and first stated priority, so the empty workspace does not just say what to do next; it says why that is next for this pastor.
- Conversational next-check continuity: passed. "Who should I check on next?" now looks at the recent chat thread for a named member and prioritizes that member's active care/prayer item when present, so a pastor can ask a natural follow-up after discussing someone instead of getting only a cold global sort. `scripts/smoke_first_run.py` covers a competing active care case to keep this behavior explicit.
- Natural post-visit logging: passed. Chat now treats phrases like "I just got back from visiting Janet Ellis at home" as a pastoral contact update, saves it against the active care case, resets the care follow-up timer, and returns the linked care card instead of requiring command-like "Log that I visited..." wording.
- Secret-pasting guardrail: passed. First-run chat now routes questions like "Should I paste my Gmail password or API key here?" to secure connector guidance that explicitly says not to paste passwords, OAuth tokens, refresh tokens, API keys, or client secrets into chat, and points API-key connectors to encrypted credential setup.
- First-week plan personalization: passed. `scripts/smoke_first_run.py` now verifies the queued first-week launch plan review-card description and plan payload carry the pastor's saved support style, not just tasks, connector setup, rhythm, church voice, and approval guardrails.
- Weekly help prompt routing: passed. `scripts/smoke_first_run.py` now verifies both "How can you help me this week?" and "How will you help me this week?" route to the saved ministry operating plan, attach the concrete first-record setup card while the workspace is still empty, and stay anchored in the pastor's follow-up burden and first stated priority instead of falling into a generic context-use explainer.
- Pastoral pressure prompt routing: passed. `scripts/smoke_first_run.py` now verifies "I am overwhelmed by all the follow-up today" routes to `pastor_support`, preserves the saved support preference and weekly rhythm, and attaches the smallest concrete next setup item instead of falling into generic chat encouragement.
- Load-relief phrasing: passed. "What can you take off my plate today?" now routes to the same pastoral support path, preserves the saved support style, explains reviewable delegation/approval boundaries, and attaches the smallest concrete next setup item instead of answering like generic chat.
- Next-action personalization: passed. `scripts/smoke_first_run.py` now verifies "What should I handle next?" stays grounded in real people or queued review work while preserving the pastor's saved support style.
- Deferral triage personalization: passed. `scripts/smoke_first_run.py` now verifies "What can wait until next week?" triages real people/review work, keeps approval boundaries visible, and preserves the pastor's saved support style.
- Staff-meeting attention prompt: passed. "What needs my attention before staff meeting?" now routes to the populated morning desk briefing, so the research example returns concrete people/review work, support style, and approval boundaries instead of generic prioritization.
- Connect-first prompt routing: passed. `scripts/smoke_first_run.py` now verifies "What should I connect first?" starts the saved-tool connector setup, chooses Google Workspace from a visitor-follow-up/Gmail profile, explains the recommendation from the saved follow-up burden, names Planning Center as the next saved connector, keeps secure setup boundaries visible, and does not suggest syncing before connector setup plus credential checks are complete. It also verifies a hospital/grief care profile with the same Planning Center/Gmail stack prioritizes Planning Center over Gmail while still naming Google Workspace as the next communication connector.
- First visitor draft personalization: passed. The first-run smoke asserts a visitor note such as "asked about kids ministry" appears in the queued welcome draft body, while the saved drafting voice, church voice/tradition, and approval guardrails stay in review metadata rather than provider-send payload text.
- Live capture guidance specificity: passed. `scripts/smoke_first_run.py` now verifies calendar, visitor-missing-name, person-capture, and care-capture guidance do not teach first-run pastors with fake names or `.test` email addresses.
- Prayer and care draft personalization: passed. The first-run smoke asserts request-level private-prayer drafts include the actual prayer context, care drafts include the actual grief/visit context, and both action payloads carry saved drafting voice, church voice/tradition, and privacy/approval guardrails as review metadata.
- Chat morning briefing: passed. "Give me my morning briefing" returns a dedicated `morning_briefing` response; complete-but-empty workspaces get the first-real-record card instead of fake all-clear copy and reflect the saved support style; populated workspaces get named people/review work plus approval boundaries and still preserve the pastor's support style.
- Empty inbox and no-priority chat states: tightened. Complete-but-empty workspaces no longer tell pastors to sync the inbox before a Google Workspace or Microsoft 365 mailbox has completed secure setup plus a no-sync credential check; empty inbox/reply requests now attach mail setup/check cards, and no-priority "what needs attention" replies use setup steps, saved rhythm, or a request for the next real person instead of generic admin-work language.
- Empty connected-context lookups: tightened. "Show Planning Center context" and missing synced-person import requests now explain secure setup, no-sync credential check, then sync order, attach the requested connector setup/check card, and avoid provider-sync prompts before credentials are checked.
- Care visit scheduling before real context: tightened. "Where can I fit care follow-up?" now asks for the real person or active care case first and keeps the first-record setup card attached, instead of falling through to a generic assistant response or protected-work calendar block.
- Deferral/next-action wording: tightened. "What can wait until next week?" no longer names generic admin cleanup as pastoral triage; it keeps deferrable work tied to real visitors, care cases, prayer requests, absence check-ins, connector order, and approved external actions. Empty next-action fallback now asks for the next real ministry update or preserves the saved rhythm instead of generic sermon/admin work.
- Legacy chat compatibility: passed. `scripts/smoke_first_run.py` now asserts `POST /chat/` routes through the connected assistant path, persists the pastor turn and assistant reply metadata in `/assistant/chat/history`, saves the named visitor, avoids `Guest` placeholders, and queues the reviewable welcome draft.
- Legacy briefing defaults: cleaned up. `GET /briefing/today` and `scripts/morning_briefing.py` no longer default to the old Nathan/Hallmark demo identity or produce doubled `Pastor Pastor` greetings; the cron script can target a real workspace with `MARGE_ACCOUNT_TOKEN`, `MARGE_ACCOUNT_ID`, or `MARGE_ACCOUNT_SLUG`.
- API-key connector transport: tightened. Workspace Breeze/Rock credential setup now rejects unsafe base URLs before storing encrypted API-key credentials; chat/setup cards say `Add encrypted credentials` when encrypted workspace storage is ready and fall back to encrypted-storage/server-config guidance when it is not; the frontend requires and labels the Breeze/Rock public HTTPS base URL before credential submission; server-side `BREEZE_BASE_URL` and `ROCK_BASE_URL` must be clean public HTTPS base URLs with no username, password, query string, fragment, localhost, or private-network host in production config verification, runtime startup checks, connector status, and verification/sync paths.
- OAuth callback status boundary: passed. `scripts/smoke_integrations.py` and `scripts/smoke_connected_providers.py` now verify OAuth callback pages and unverified Google Workspace, Planning Center, and Microsoft 365 integration status hints tell pastors to run Check credentials before syncing ministry data, even though the encrypted OAuth token is connected.
- Chat connector setup boundary: passed. `scripts/smoke_integrations.py` now verifies that after Google OAuth callback but before credential verification, a pastor's "Connect Google Workspace" chat prompt returns a `Check Google Workspace credentials` card and does not suggest syncing or start another setup step; following the suggested "Check Google Workspace credentials." prompt runs a no-sync verification through chat, records `verified_at`, imports no connected context, and queues no actions instead of being saved as onboarding/profile context.
- Live no-sync evidence coverage: tightened. `scripts/verify_live_integrations.py` now paginates side-effect snapshots across connected context, assistant actions, members, visitors, care cases, and prayer requests, and includes private prayer requests, so a busy pilot workspace cannot hide credential-check imports outside the first 200 rows. `scripts/smoke_connected_providers.py` covers the pagination and private-prayer snapshot behavior.
- Calendar write readiness boundary: passed. `scripts/smoke_integrations.py` now verifies a connected-but-unverified Google Workspace calendar is not described as ready to stage writeback work; "What calendar details do you need?" returns a credential-check card until `verified_at` exists; a complete calendar-event prompt with an unchecked provider acknowledges the event details and returns credential-check cards instead of a generic missing-details reply; calendar help examples avoid fake names and `.test` email addresses; after `verified_at` exists, help switches to connected-calendar review-item guidance without setup cards.
- External write credential boundary: passed. `scripts/smoke_integrations.py` now verifies an approved Google draft still cannot write externally when church policy is enabled until the OAuth credential has passed the no-sync credential check.
- Frontend connector readiness boundary: passed. `scripts/smoke_frontend_static.py` now verifies integration cards and the Today detail connector-health panel label unchecked credentials as "Needs check", setup/chat `Check credentials` cards call the no-sync verifier instead of restarting setup or dead-ending, Sync/writeback controls stay hidden until `verified_at` exists, writeback is locked until credentials pass Check credentials, and MCP is excluded from pastor-facing church-tool readiness summaries.
- User-scoped proactive setup boundary: passed. `scripts/smoke_integrations.py` now verifies `/assistant/actions/prepare` evaluates connector setup against the current pastor user's OAuth readiness, so a second pastor still gets `Connect Google Workspace` setup work instead of silently inheriting the owner's verified Google credential.
- Rock connector config: cleaned up. Server-side Rock config now uses generic `ROCK_API_KEY` plus `ROCK_BASE_URL`; production runtime checks reject the old Hallmark-specific env name, and `scripts/smoke_integrations.py` asserts Rock setup/credential validation exposes only the generic names.
- Legacy Rock sync boundary: cleaned up. `POST /members/sync/rock` now delegates to the assistant Rock connector sync path, so it uses encrypted workspace credentials, requires credential verification before sync, and cannot silently bypass connected-context/audit behavior.
- Public root privacy: cleaned up. `GET /` no longer reads `PASTOR_NAME` or `CHURCH_NAME`; `scripts/verify_deployment_bootstrap.py` now asserts the root response is a generic app pointer and does not expose pastor/church identity before signup.
- Absence draft prompt: passed. The backend-suggested "Draft absence check-ins." prompt now queues reviewable absence `email_draft` actions from attendance context instead of repeating the absence lookup response; the smoke verifies saved pastor voice metadata is attached.
- MCP formatter redaction: tightened. `mcp_server/server.py` now redacts token/API-key/session-shaped text across saved chat history, assistant desk fields, profile fields, care/prayer/member names, connected-context labels/snippets, draft bodies, connector setup/sync/verify/disconnect messages, action cards, prompt chips, compact payloads, and API error details before external LLM clients see them. `scripts/smoke_mcp_first_run.py` includes a no-network formatter smoke that injects token-shaped values through those surfaces and fails if any leaked value remains.
- `.venv/bin/python -m compileall mcp_server/server.py scripts/smoke_mcp_first_run.py`: passed.
- Focused MCP redaction smoke: passed with `.venv/bin/python - <<'PY' ... assert_mcp_api_errors_use_safe_detail(...) ... assert_mcp_formatters_redact_secret_text(...) ... PY`.
- `.venv/bin/python scripts/smoke_mcp_first_run.py`: passed. The MCP sync precheck now formats credential-health metadata as `people access: confirmed`, does not expose `sample_people_access`, imports no ministry context, queues no actions, and asks for an explicit follow-up sync.
- `.venv/bin/python scripts/smoke_connected_providers.py`: passed. It now also asserts unverified Planning Center and Microsoft 365 OAuth callback/status copy keeps the credential-check-before-sync boundary visible; the backend verify endpoint refuses to set `verified_at` for false-only, zero-only, or negative numeric provider metadata; chat-triggered Breeze sync stops at the failed credential check, imports no ministry data, queues no actions, and does not mark the connector checked; the live-provider readiness counter requires an explicit positive `identity_signal`, accepts fully verified external providers, and rejects MCP, missing `verified_at`, non-`verified` statuses, empty identity metadata, false-only or zero-only credential-health metadata, secret-shaped identity keys, and sample/demo-labeled Breeze credential metadata. It also covers the live-provider workspace-token guard for local and non-local APIs, confirms an unreachable API returns a clean operator error instead of a Python traceback, verifies the live-provider verifier prints next actions when only MCP/no external provider qualifies, and protects generic calendar sync so Planning Center remains the preferred connected calendar source even though connect-first setup recommendations can be ministry-context-aware.
- `.venv/bin/python scripts/smoke_integrations.py`: passed. It now also asserts unverified Google Workspace OAuth callback/status copy keeps the credential-check-before-sync boundary visible, chat-triggered connector setup returns a credential-check card instead of sync/setup-again guidance while `verified_at` is missing, following the suggested credential-check prompt verifies through chat without syncing/importing/queueing actions, unverified Google calendar write help and complete calendar-event prompts return a credential-check card instead of write-ready or missing-details copy, calendar help avoids fake names and `.test` email addresses, verified Google calendar write help returns connected-calendar guidance without setup cards, approved external writeback is blocked until the credential check records `verified_at`, `/assistant/actions/prepare` uses the current pastor user's OAuth readiness instead of another workspace user's verified credential, Rock setup cards/chat prompt for encrypted workspace credentials when Fernet storage is available, and Rock workspace API-key credentials reject non-HTTPS base URLs, localhost/private-network hosts, and URLs that smuggle username/password/query data before secure storage.
- `.venv/bin/python scripts/smoke_frontend_static.py`: passed. It now also asserts Breeze/Rock API-key setup forms require a public base URL, show `Add encrypted credentials` when encrypted workspace storage is available, show `Review encrypted storage` instead of generic server-config wording when `MARGE_ENCRYPTION_KEY` is missing, keep chat response actions/suggested prompts visible after the post-chat data refresh, render those returned action cards/prompts in secondary-view `Tell Marge` panels instead of text-only bubbles, keep every pre-workspace live chat composer on local setup-safe replies instead of calling scoped assistant chat without a workspace, attach a visible `Create workspace` card plus setup prompt chips to those local replies, and require pastor name, email, and church name in the visible workspace signup form.
- `.venv/bin/python -m compileall scripts/smoke_pilot_readiness_gate.py scripts/verify_pilot_readiness.py`: passed.
- `.venv/bin/python scripts/smoke_pilot_readiness_gate.py`: passed. This no-network smoke verifies `.env.example` uses the generic Rock env name, warns that env placeholders/MCP are not live-provider proof, and keeps Check credentials before first sync visible; verifies `.env.production.example` includes every production verifier key except the alternate enforcement flag and the legacy Rock fallback; verifies production env-file checks do not let ambient shell values mask missing production-critical keys; verifies missing production config prints actionable `next_actions`; verifies a complete candidate production env file still passes when the required values are present in the file while keeping live-provider setup visible as a warning; verifies app/CORS origin mismatches fail both production config verification and runtime guard checks; verifies OAuth callback origin/path mismatches fail both production config verification and runtime guard checks; and verifies unsafe Breeze/Rock server-side base URLs, including localhost/private-network hosts, fail both production config verification and runtime guard checks. The combined gate now skips first-run writes and live connector checks until `MARGE_ACCOUNT_TOKEN` is present; non-local tokens must resolve to owner/admin/pastor access; invalid and staff-role tokens are rejected; blank env-file `MARGE_ACCOUNT_TOKEN=` and `MARGE_API_URL=` values do not erase non-empty shell-provided operator values; explicit CLI flags still override env-file and shell values; production-config failures keep a copy-pasteable `.venv/bin/python scripts/verify_production_config.py` rerun command in the combined summary; the live verifier receives the default or custom evidence file path; the combined gate validates the generated evidence JSON and fails when it does not prove `live_provider_ready=true` plus no-sync side-effect safety; and a failed live connector check keeps the combined gate failed with a copy-pasteable `.venv/bin/python scripts/verify_live_integrations.py --include-mcp --require-live-provider --evidence-file artifacts/live-connector-verification.json` rerun command.
- `.venv/bin/python -m compileall app mcp_server scripts`: passed.
- `.venv/bin/python -m compileall app/routers/assistant.py scripts/smoke_first_run.py`: passed after the empty-inbox/no-priority hardening.
- `.venv/bin/python scripts/smoke_first_run.py`: passed after adding assertions for "What is in my inbox?" and "Queue replies for these." before mail setup; both now require secure mailbox setup plus credential checks and attach Google Workspace/Microsoft 365 setup cards without suggesting pre-verification sync. It also now asserts "Where can I fit care follow-up?" before a real care person exists returns care-capture guidance with the first-record setup card instead of generic calendar work.
- First-run sync prompt hygiene: tightened. Open integrations, explicit pre-setup inbox/calendar/provider sync requests, empty meeting-prep lookup, empty absence lookup, and empty absence-draft prompts no longer suggest syncing connected tools, mailbox, calendar, Planning Center, Google Workspace, Breeze, Microsoft 365, or Rock RMS before the relevant connector has completed secure setup and a no-sync credential check; they attach setup/check cards instead.
- Cached connected-context refresh prompts: tightened. When Marge has previously synced context but the provider is later disconnected or no longer credential-checked, chat still shows the cached review context but swaps `Sync {provider} again` for reconnect/check guidance and a setup/check card.
- Cached inbox/calendar refresh prompts: tightened. Generic inbox lookup, queued synced-inbox replies, and meeting-prep lookup now use the same credential-checked refresh gate, so cached Google/Microsoft/Planning Center calendar or email context remains visible after disconnect but no longer suggests mailbox/calendar sync until setup and the no-sync credential check are restored.
- Failed verify-before-sync guidance: tightened. If a chat-triggered sync stops to perform the no-sync credential check and that check fails, Marge now returns the provider credential-check card and exact check prompt instead of a text-only failure, while still confirming no ministry data was imported and no actions were queued. `scripts/smoke_integrations.py` covers the Google Workspace failure path.
- Ministry-profile prompt loop: tightened. `Open the ministry profile.` now returns the saved profile/operating-plan recap without suggesting the same prompt again; follow-up chips move the pastor toward church-context recap, context usage, or next action. `scripts/smoke_first_run.py` covers this suggested prompt.
- Empty prepare-meeting prompt: tightened. `Prepare my next meeting.` now reuses the credential-aware meeting-prep empty route when no synced calendar event exists, attaching saved calendar setup/check cards and avoiding calendar sync prompts before setup plus the no-sync credential check. `scripts/smoke_first_run.py` covers the pre-calendar first-run case.
- Generic connected-context wording: tightened. Generic synced-context chat now says `connected tool context` instead of the placeholder-like `connected-tool context`; `scripts/smoke_connected_providers.py` covers the generic "Show connected context." route after synced provider data exists.
- Live briefing empty-state wording: tightened. `GET /briefing/today` no longer uses the universal `Here are your people for today` greeting or `well-tended` empty-state language for live first-run workspaces without records; it now says what Marge can see in the current workspace and asks for real ministry context before sorting people. `scripts/smoke_first_run.py` covers the live briefing response, and `scripts/smoke_frontend_static.py` guards the live Today header fallback.
- Empty AI-briefing prompt: tightened. When an LLM key is enabled but the live workspace has no real records yet, `generate_ai_briefing` now tells the model that the current workspace lacks real ministry context and forbids invented people or clear-desk conclusions instead of prompting with "No urgent pastoral needs flagged today." `scripts/smoke_first_run.py` captures the generated prompt with a fake provider and asserts the honest empty-context guidance is present.
- Frontend live empty states: tightened. Live Today/detail panes now say no current follow-up, draft, calendar, or situation item is visible from the current workspace context instead of using "No urgent care gaps", "No urgent follow-up gaps", `Clear`, or "No urgent situation logged" language. `scripts/smoke_frontend_static.py` guards the replacement copy.
- Missing-name briefing/UI fallbacks: tightened. Live briefing text, AI briefing prompts, and frontend rows now use `Name not linked` or `Name withheld` for incomplete/private records instead of `Unknown`, `Anonymous`, or generic `friend` fallbacks. `scripts/smoke_first_run.py` covers rendered and AI briefing prompts, and `scripts/smoke_frontend_static.py` covers the frontend script.
- Connected-inbox missing-sender fallbacks: tightened. Synced inbox triage now says `Sender not available` instead of `Unknown sender`, and connected email reply drafts greet an unparsed sender with `Hi there` instead of `Hi friend`. `scripts/smoke_first_run.py` covers these helper paths without requiring a live provider.
- Connected-context missing-payload copy: tightened. Care lines now say `Name not linked` and `description not attached yet`; inbox triage says the provider did not include a preview; connected meeting prep says the connected calendar did not include time/description and asks for pastor context instead of using `time not listed`, `No description synced`, or generic saved-context placeholders. `scripts/smoke_first_run.py` covers these helper paths.
- Calendar write setup prompts: tightened. Complete calendar-event prompts before a credential-checked calendar now derive suggested prompts from the attached setup/check card, so a first-run workspace suggests `Connect Google Workspace` before setup exists and `Check Google Workspace credentials` only after setup exists. `scripts/smoke_first_run.py` covers both helper states.
- Calendar details help prompts: tightened. `What calendar details do you need?` keeps the concrete event-format example first, then suggests the actual setup/check next step from the attached calendar connector card instead of hiding setup behind only `Open integrations`. `scripts/smoke_first_run.py` covers the helper and first-run route.
- Backend proactive empty-state copy: tightened. The desk proactive summary now says no current follow-up items are visible in this workspace instead of `No urgent people are flagged`, while still preserving saved rhythm and support-style context. The live sidebar also uses `None visible` instead of `Clear`. `scripts/smoke_first_run.py` and `scripts/smoke_frontend_static.py` cover both paths.
- Suggested setup prompt routing: tightened. Chat now treats backend-suggested prompts such as `Start Google Workspace setup.` as secure connector setup commands, so Marge's own prompt chips do not fall through to generic chat.
- Generic email/calendar setup phrasing: tightened. Chat now treats natural pastor wording such as `connect my email`, `set up my mail`, `connect my calendar`, and `set up my schedule` as secure connector setup intent, then chooses Google Workspace or Microsoft 365 from the pastor's saved tools instead of requiring the pastor to name the provider in the prompt. `scripts/smoke_first_run.py` covers the helper path.
- First-tool setup copy: tightened. The fallback `Connect the first ministry tool` setup card now includes Microsoft 365 alongside Google Workspace, Planning Center, Rock RMS, and Breeze, so Outlook churches are represented even before the pastor has named a preferred provider. `scripts/smoke_first_run.py` covers the helper path.
- Synced-context empty-state copy: tightened. The frontend no longer tells pastors to connect only Google Workspace, Planning Center, or Rock RMS before syncing; it now includes Microsoft 365 and Breeze and explicitly keeps Check credentials before sync. `scripts/smoke_frontend_static.py` covers the visible empty state.
- Onboarding tool-name copy: tightened. Browser and backend onboarding copy now pairs pastor-friendly names with actual connector names (`Gmail/Google Workspace`, `Outlook/Microsoft 365`) instead of saying only Google/Outlook while the secure setup routes use provider names. `scripts/smoke_frontend_static.py` and a focused `scripts/smoke_first_run.py` helper assertion cover the visible/backend placeholders.
- Onboarding tool extraction: tightened. Chat/profile extraction now canonicalizes Gmail plus Google Workspace into one saved `Gmail/Google Workspace` entry, and Outlook plus Microsoft 365 into one saved `Outlook/Microsoft 365` entry, so first-run setup recommendations do not carry duplicate aliases for the same connector. `scripts/smoke_first_run.py` covers the helper path and updated first-run expectations.
- Backend/frontend missing-name fallbacks: tightened. Priority care, visitor, and absence cards now use incomplete-context labels for unlinked records; connected person imports preserve a first-name-only provider payload without inventing an `Unknown` last name; Rock RMS sync skips no-name provider rows instead of creating placeholder members; and the frontend action modal uses `Name not linked`, `Sender not available`, and provider-incomplete preview wording. `scripts/smoke_first_run.py` and `scripts/smoke_frontend_static.py` cover these helper/static paths.
- Signup email placeholders: tightened. Visible first-run email fields now use `you@yourchurch.org` instead of `pastor@example.com`, and `scripts/smoke_frontend_static.py` guards against generic sample email placeholders in the signup/login shell.
- User-scoped setup-card status: tightened. `_seed_context_step` and missing-visitor-name guidance now pass the active workspace user into integration-status calculation, so setup cards shown from data-seed/help paths do not accidentally reflect another user's OAuth credential state. `scripts/smoke_first_run.py` covers this helper path.
- Ministry memory recap: tightened. `What have you learned about my ministry?` now explicitly includes saved drafting voice, weekly rhythm, and approval guardrails in addition to role/church, local context, church voice/tradition, follow-up burden, first priority, support style, and tools. `scripts/smoke_first_run.py` guards that the recap includes the pastor's saved voice/rhythm/approval boundaries instead of burying them in setup cards.
- Ministry learning gaps: tightened. Completed-profile prompts such as `What else do you need from me?` now answer with `ministry_learning_gaps`, distinguish the learned core profile from the next missing real ministry record or live connector context, attach the concrete `data_seed`/connector setup cards, and repeat the setup -> no-sync credential check -> pastor-requested sync order. `scripts/smoke_first_run.py` covers the route-level first-run path.
- Profile setting lookups: tightened. `What are my guardrails?` now answers from saved profile guardrails while preserving checked-credentials/writeback-policy/exact-approval boundaries; `How should you sound when drafting?` answers from saved drafting voice plus church voice/tradition; `What rhythm should you protect?` answers from saved weekly rhythm while preserving external-calendar approval boundaries; and `What tools do you remember we use?` answers from saved tools while attaching connector setup/check cards and repeating setup -> no-sync credential check -> pastor-requested sync order. `scripts/smoke_first_run.py` covers these route-level first-run prompts.
- Pastoral reminder chat: implemented. Natural secretary phrasing such as `Remind me to call Janet Ellis tomorrow` now queues a local `pastoral_reminder` review item through `/assistant/chat`, links known local people, preserves timing context, and states that nothing was sent, synced, or written externally. `scripts/smoke_first_run.py` covers both the helper path and the route-level first-run chat path.
- Pastoral reminder lookup: implemented. Natural lookup prompts such as `What reminders do I have?` now list pending local `pastoral_reminder` actions with reminder cards and local-memory guardrail copy instead of falling into generic approval-queue language. `scripts/smoke_first_run.py` covers helper and route-level paths.
- Proactive contact follow-up reminders: tightened. After chat logs a pastoral contact, Marge now suggests a local next-check-in reminder such as `Remind me to check on Janet Ellis next week.` instead of ending at a saved note/contact. This remains local Marge memory, not an automatic external calendar write. `scripts/smoke_first_run.py` covers helper and route-level paths.
- Pastoral reminder completion/canceling/rescheduling: tightened. Pending local reminder actions now render a `Mark done` path and can be completed directly through `/assistant/actions/{id}/execute` or natural chat commands such as `Mark Janet reminder done` without requiring a separate approval, because this is local Marge memory rather than an external write. Natural cancel phrasing such as `Cancel Janet reminder` skips the matching local reminder, while `Move Janet reminder to Friday` and `Snooze it in two weeks` update only the local reminder timing. These paths do not touch external systems. `scripts/smoke_first_run.py` and `scripts/smoke_frontend_static.py` guard the behavior and local-memory copy.
- Remembered member preferences: implemented. Natural memory phrasing such as `Remember that Janet Ellis prefers phone calls over texts` now saves a local `MemberNote` tagged as `preference` when the person is matched, stays out of profile context, returns a visible member-note card, and later person-context lookups surface it as `Preferences to respect`. `scripts/smoke_first_run.py` covers both helper and route-level chat paths.
- Person-detail preference UI: tightened. The frontend person detail panel now renders `context_tag="preference"` notes under `Preferences To Respect`, separate from `Recent Notes`, so remembered relational preferences do not get buried. `scripts/smoke_frontend_static.py` guards the static UI behavior.
- Draft local-memory review metadata: tightened. Care/prayer/absence draft actions for matched members now include active care/prayer context in `payload.draft_context.member_context` plus remembered `preference` notes in `payload.draft_context.member_preferences`; the approval modal renders that context before the sendable draft. The draft body stays clean unless the pastor explicitly edits sensitive details into it. `scripts/smoke_first_run.py` and `scripts/smoke_frontend_static.py` guard the behavior.
- Pilot connector runbook: added. `docs/PILOT_CONNECTOR_RUNBOOK.md` now gives operators a concrete first-live-provider path: choose the provider from the pastor's saved ministry burden, configure secure OAuth/API-key setup, run the no-sync credential check, verify with `scripts/verify_live_integrations.py --include-mcp --require-live-provider`, capture evidence, and stop if MCP/OAuth callback/setup-only readiness is being mistaken for a verified live provider.
- Live verifier evidence artifact: tightened. `scripts/verify_live_integrations.py` now accepts `--evidence-file` to write a structured JSON report for pilot handoff, including early missing-token/unreachable-API failures, without requiring shell redirection or including workspace/provider secrets. `scripts/smoke_connected_providers.py` covers the missing-token evidence path and verifies next actions are preserved.
- Live verifier evidence redaction: tightened. `scripts/verify_live_integrations.py` now redacts obvious token/API-key/session/JWT-shaped values in provider error messages, early failure errors, next actions, and evidence JSON before returning or writing the report. `scripts/smoke_connected_providers.py` covers the redaction helpers and evidence payload path.
- Backend verify identity-value guard: tightened. `POST /assistant/integrations/{provider}/verify` now rejects token/API-key/session/JWT-shaped identity values, not only secret-shaped keys, before returning metadata to the browser or setting `verified_at`. `scripts/smoke_connected_providers.py` patches a provider verifier to prove leaked identity values are blocked without echoing the secret.
- Frontend verify metadata guard: tightened. The connector verification modal now filters token/API-key/session/JWT-shaped identity keys or values before rendering provider metadata, and redacts token-shaped free-text verify/error messages before showing them, adding a browser-side defense if a future API response regresses. `scripts/smoke_frontend_static.py` covers the static UI guard.
- Frontend execution-link guard: tightened. Provider execution summaries now redact token-shaped provider IDs, hide unsafe non-HTTPS or token-bearing external links, and redact server-provided chat-history messages before showing toasts. `scripts/smoke_frontend_static.py` covers the static UI guard.
- Backend chat failure redaction: tightened. Connector verify/sync/action-execution failure replies now pass provider and HTTP exception text through `_redact_secret_text()` before Marge says it in chat. `scripts/smoke_connected_providers.py` forces a Breeze credential-check failure with an `apiKey=...` detail and verifies the chat reply redacts it.
- Live verifier MCP evidence boundary: tightened. `scripts/verify_live_integrations.py` now separates external provider credential checks from local MCP bridge checks in human output, writes typed JSON arrays `external_provider_checks` and `local_bridge_checks`, records `local_bridge_checked_count` separately from `external_verified_count`, includes `generated_at`, token-free `workspace` scope, explicit `live_provider_ready`, and `no_sync_side_effect_check_passed` fields, and avoids a generic `Verified` section that could make MCP-only checks look like church-tool readiness. `live_provider_ready` is true only when a supported external provider verifies and the no-sync side-effect check actually passes; skipped or missing side-effect checks cannot satisfy readiness, and human output now says when that proof did not run. `scripts/smoke_connected_providers.py` includes focused assertions for MCP-only human output, typed evidence arrays, readiness booleans, side-effect failure or absence, bridge counting, and generated evidence timestamps.
- Standalone live-verifier role guard: tightened. `scripts/verify_live_integrations.py --require-live-provider` now rejects non-owner/admin/pastor workspace roles before provider checks, matching the combined pilot gate's operator-token role boundary. `scripts/smoke_connected_providers.py` covers owner acceptance and staff rejection through the verifier helper.
- Standalone live-verifier rerun guidance: tightened. When `scripts/verify_live_integrations.py` is run with `--evidence-file`, all strict live-provider `Next actions` rerun commands, including early missing-token failures, now preserve that evidence path instead of drifting back to stdout-only verification. `scripts/smoke_connected_providers.py` covers no-live-provider, workspace-required, and early missing-token rerun guidance.
- Combined readiness evidence validation: tightened. `scripts/verify_pilot_readiness.py` now clears any previous evidence file, passes `--evidence-file artifacts/live-connector-verification.json` to `scripts/verify_live_integrations.py` by default, accepts `--live-evidence-file <path>` for custom artifact locations, and validates the generated JSON before the combined gate can pass. The evidence `api_url` must match the current `MARGE_API_URL`, must say `required_live_provider=true`, must include timezone-bearing `generated_at` from the last 24 hours and not future-dated beyond clock skew, must include token-free workspace scope with owner/admin/pastor role, must say `live_provider_ready=true`, must say `no_sync_side_effect_check_passed=true`, must include complete passing `side_effect_check.collections` for connected context, assistant actions, members, visitors, care cases, and prayer requests, and must include a verified supported external provider check for Google Workspace, Microsoft 365, Planning Center, Breeze, or Rock RMS; MCP/local bridge data, secret-shaped keys or obvious secret-shaped values anywhere in the report, missing side-effect collection details, or the legacy `verified` array cannot satisfy the gate. `scripts/smoke_pilot_readiness_gate.py` covers default/custom evidence paths, stale evidence cleanup, non-strict evidence rejection, missing, timezone-less, malformed, stale, or future-dated `generated_at`, API URL mismatch rejection, missing workspace scope rejection, non-pastoral role rejection, missing/failed no-sync side-effect collection details, unsupported-provider rejection, sensitive identity-key rejection, secret-shaped evidence-key/value rejection, and invalid evidence blocking the gate.
- Local evidence artifact handling: tightened. `.gitignore` now excludes `artifacts/`, and the production/runbook docs tell operators to store generated live-provider evidence in the deployment or pilot handoff system rather than committing workspace/provider metadata to git.
- Operator evidence docs: tightened. `docs/PILOT_CONNECTOR_RUNBOOK.md` and `docs/PRODUCTION_READINESS.md` now name the required typed evidence fields (`required_live_provider`, `generated_at`, `workspace`, `external_provider_checks`, `local_bridge_checks`, `external_verified_count`, `live_provider_ready`, `no_sync_side_effect_check_passed`, and `side_effect_check.collections`), state that the combined pilot gate clears stale evidence before running, and warn that MCP/local bridge checks plus the legacy `verified` array cannot prove a live church-tool provider.
- Top-level evidence docs: tightened. `README.md`, `BUILD_SUMMARY.md`, and `mcp_server/README.md` now point production/pilot handoff toward `--evidence-file artifacts/live-connector-verification.json`, typed timestamped `external_provider_checks`/`local_bridge_checks` evidence, stale-evidence clearing, and the rule that legacy `verified` data or MCP bridge checks cannot satisfy live-provider readiness.
- Pastor-facing agent copy: tightened. Shared `/app` navigation headers now describe external/approved AI assistants and separate agent access without leading pastors with MCP protocol language; the technical MCP command remains on the Agent Tools setup page. `scripts/smoke_frontend_static.py` guards against `MCP stays under Agent Tools` and `MCP-capable assistants should act through` returning to first-run navigation copy.
- Synced inbox local-memory metadata: tightened. Google Workspace/Microsoft 365 reply drafts now try to match the sender to local Marge people memory by email or full display name, then attach active care/prayer context to `payload.draft_context.member_context` plus remembered preferences to `payload.draft_context.member_preferences` without adding them to the sendable body. `scripts/smoke_first_run.py` covers the helper path.
- Synced meeting local-memory metadata: tightened. Google Workspace/Microsoft 365 meeting-prep actions now try to match organizers/attendees to local people memory by email or full display name, attach active care/prayer context to `payload.review_context.member_context`, attach remembered preferences to `payload.review_context.member_preferences`, and the approval modal reads review context as well as draft context. `scripts/smoke_first_run.py` and `scripts/smoke_frontend_static.py` guard the behavior.
- Pre-meeting person context: tightened. Natural questions like `What should I know before meeting with Janet Ellis?` now route to person context and include matching synced calendar events alongside local care/prayer/notes/preferences, with synced calendar cards returned for review. `scripts/smoke_first_run.py` covers the helper path.
- Pastoral history lookup: tightened. Natural questions like `When did I last visit Janet Ellis?` now route to person context before generic calendar planning and answer from saved care/contact memory. `scripts/smoke_first_run.py` covers helper and route-level paths.
- Who-to-check-on triage: tightened. Natural questions like `Who should I check on next?` now route to next-action triage and answer from current ministry context instead of generic advice. When the top item is a named care case, suggested prompts carry that person into a care draft and local reminder. `scripts/smoke_first_run.py` covers helper and route-level paths.
- Synced row action prompts: tightened. Frontend synced inbox and synced calendar rows no longer fall back to a generic approval-queue button when no action is attached; they prompt Marge to draft a reviewable reply or prepare a meeting brief from that real connected item. `scripts/smoke_frontend_static.py` guards the static UI behavior.
- MCP missing-name copy: tightened. `mcp_server/server.py` now lists incomplete member rows as `Member name not linked`, unlinked care cases as `Name not linked`, and private/unnamed prayer requests as `Name withheld`, so external LLM clients do not see `Unknown`, `Anonymous`, or raw Python `None` placeholders. `scripts/smoke_mcp_first_run.py` includes focused MCP helper assertions for these list outputs.
- MCP safe API errors: tightened. MCP tool failures now format backend `detail` through `_http_error_detail` instead of returning raw JSON response bodies or transport strings to external LLM clients. MCP tool failures, connector messages, identity summaries, compact execution payloads, and generic exception text now redact token/API-key/session-shaped values before returning text to external LLM clients. `scripts/smoke_mcp_first_run.py` includes focused helper assertions for safe detail formatting and redaction.
- MCP incomplete connected-context/action copy: tightened. MCP connected-context and approval-list output now says `title not included by provider`, `date not included by provider`, or `Assistant action title not included` instead of `Untitled` or `unknown date`. `scripts/smoke_mcp_first_run.py` includes a focused helper assertion for these fallback paths.
- MCP actionable empty states: tightened. Empty MCP member, care, prayer, connected-context, and approval-list tools now point external LLM clients toward the first real ministry record, secure connector setup/check/sync order, or reviewable work instead of dead-ending on `No X found` copy. `scripts/smoke_mcp_first_run.py` includes a focused helper assertion for these empty paths.
- MCP named prayer preservation: tightened. `add_prayer_request` now preserves an unmatched provided name as `submitted_by` and tells the agent the request is not linked to a saved member yet, instead of silently saving a named prayer request as unnamed/private context. `scripts/smoke_mcp_first_run.py` includes a focused helper assertion for this path.
- MCP unmatched-member write safety: tightened. MCP care-event, member-note, and draft tools now refuse unmatched names without mutating data and point the agent to safe person capture through `tell_marge`, instead of dead-ending on `Try list_members` after the agent already supplied a real name. `scripts/smoke_mcp_first_run.py` includes a focused helper assertion for these paths.
- MCP ambiguous-member write safety: tightened. `_find_member` now allows exact full-name matches or single-result searches only, so MCP write tools do not silently pick the first result from an ambiguous partial-name search. `scripts/smoke_mcp_first_run.py` includes a focused helper assertion for exact-vs-ambiguous matching.
- MCP connector boundary: tightened. `list_integrations` now labels MCP as a local agent bridge rather than a church-tool provider and states that live provider readiness still requires a verified external connector. `scripts/smoke_mcp_first_run.py` includes a focused helper assertion for MCP-plus-real-connector output.
- MCP backend verification boundary: tightened. `/assistant/integrations/mcp/verify` now reports local bridge availability instead of credential verification, returns `verified_at=null`, does not persist `verified_at` for MCP, and says it does not prove an external provider is connected. `scripts/smoke_integrations.py` includes assertions for this route.
- MCP tool description copy: tightened. The `tell_marge` tool schema no longer teaches external LLM clients with fake people such as Martha, Tom Henderson, or the Wilson family; it tells clients to use the real person, family, or ministry situation the pastor provides. `scripts/smoke_mcp_first_run.py` includes a focused assertion for this schema text.
- Focused MCP backend boundary smoke: passed. A temporary workspace called `/assistant/integrations/mcp/verify`, confirmed `bridge_available` wording, injected a stale MCP connection row with old credential-verified copy, and confirmed `/assistant/integrations` still hid `verified_at` and returned the local-agent-bridge hint.
- `.venv/bin/python scripts/smoke_first_run.py`: passed after adding an assertion that deferral triage does not use generic "admin cleanup" language.
- `.venv/bin/python scripts/verify_deployment_bootstrap.py --allow-relaxed-account-tokens`: passed locally. This proves `/health`, `/assistant/config`, and `/app` are reachable in local dev; production readiness still requires the same script without relaxed token mode so `/assistant/config` reports `require_account_token=true`.
- `.venv/bin/python scripts/verify_first_run_workspace.py`: passed locally. It also caught and now guards against a real chat routing bug where "Show visitors needing follow-up." was incorrectly handled as a care lookup. The script now uses a non-titled signup pastor name and asserts the first-run desk greeting still renders a pastoral display name such as `Pastor Ben`.
- `.venv/bin/python -m compileall scripts/verify_live_integrations.py`: passed.
- `.venv/bin/python scripts/verify_live_integrations.py --api-url https://marge.example.com --include-mcp --require-live-provider`: failed before network access because non-local credential verification now requires `MARGE_ACCOUNT_TOKEN`.
- `.venv/bin/python scripts/verify_pilot_readiness.py --skip-migrations --skip-first-run-workspace`: production config and deployment bootstrap failed for expected local-production blockers, the workspace-token preflight now fails without `MARGE_ACCOUNT_TOKEN` even on localhost, and the gate skips first-run writes plus live connector checks until a workspace token is present. It printed concrete next actions for the failed production config, deployment bootstrap, and operator-token gates.
- Role gates for pastor-controlled surfaces: passed. `scripts/smoke_integrations.py` now asserts a staff token can read the live desk and basic member/visitor directories, while receiving `403` for ministry profile edits, approval actions, pastoral briefing, pastoral drafts, member detail/notes, care, prayer, member/visitor writes, and legacy `/chat/`.
- `.venv/bin/python scripts/verify_migrations.py`: passed.
- Runtime production guard: implemented in `app/runtime_config.py`; `MARGE_ENV=production` or `MARGE_ENFORCE_PRODUCTION_CONFIG=true` now makes startup fail closed if production guardrails, placeholder domains, partial connector config, or non-HTTPS OAuth redirect URIs are present.
- `.venv/bin/python scripts/verify_live_integrations.py --api-url http://127.0.0.1:8000 --include-mcp --require-live-provider`: failed before provider access because `--require-live-provider` now requires `MARGE_ACCOUNT_TOKEN` even for a local API. The error explains that OAuth state, API-key credentials, verification timestamps, and no-sync side-effect checks must be scoped to the pilot church workspace.
- Synthetic production config verification: passed with an exact non-placeholder `MARGE_APP_URL=https://.../app`, origin-only `CORS_ORIGINS` that includes the app origin, and same-origin OAuth callback URIs; failed when `/app` was missing, CORS included a path, CORS omitted the app origin, placeholder example domains were used, OAuth redirect config was partial/non-HTTPS, OAuth redirects used another origin, or OAuth redirects used the wrong callback path.
- Frontend inline script parse with Node: passed. The static smoke also asserts form failures surface backend-safe error detail, connector credential checks stay no-sync/no-action, unchecked credentials do not appear ready for sync/writeback, pastor-facing readiness excludes MCP, live drafts keep pointing through server/workspace records, care-focused setup prompts open the first care case instead of a generic person prompt, and live first-run copy avoids database/demo wording.
- Frontend live prompt fallback: passed. The static smoke now asserts Assistant fallback chips derive from workspace context instead of hardcoded "before noon" / "draft replies" operational placeholders when `desk.suggested_prompts` is absent.
- First-run browser answer chips: tightened. The onboarding prompt chips no longer offer fake `Pastor Ben` or `Grace Community Church` answers. When Marge asks for pastor or church name, the browser uses the actual workspace profile value when present and otherwise skips the canned answer chip. `scripts/smoke_frontend_static.py` guards the static UI text.
- Tools answer chip: tightened. The browser no longer offers a canned `Planning Center and Gmail/Google Workspace` answer for the tools question, because one-clicking the wrong stack would create false connector setup context. `scripts/smoke_frontend_static.py` guards the visible/static copy.
- Canned ministry answer chips: tightened. The browser no longer offers one-click canned answers for role, attendance, church context, follow-up burden, priorities, support style, drafting voice, rhythm, or guardrails. Those fields must come from the pastor's actual answer instead of saving sample ministry context. `scripts/smoke_frontend_static.py` guards the static UI text.
- Neutral onboarding guidance chip: tightened. When the browser has no real answer chip for the current ministry-context question, it now offers `What should I include for ...?` so the pastor can ask for guidance without saving canned context. Backend routing treats that prompt as onboarding guidance. `scripts/smoke_frontend_static.py` and a focused `scripts/smoke_first_run.py` helper assertion cover the path.
- `git diff --check`: passed.
- Browser check of live detail panel draft flow: passed.
- Browser check of first Assistant header after signup: passed.
- Browser check of pre-workspace live shell: passed. A local `/app/` load with legacy singleton rows present showed only workspace-required first-run copy, no legacy church interview question, no legacy chat, and no demo/dev wording before signup. The network list before signup contained only `/assistant/config` and `/assistant/sessions/current`, not unscoped actions, chat history, people, policies, audit, or connected-context calls. Creating "Scoped Network QA Church 20260517" through the visible form then exchanged the one-time token for an HttpOnly `marge_session`, kept `localStorage` empty, started scoped workspace data requests, and rendered the role question for the new church.
- Browser check of pre-workspace header actions: passed. The first viewport now shows `Create workspace` and `Learn plan` before signup instead of premature `Add Care` / `Add Person` write actions. After creating "Header Action QA Church 20260517", the same header actions switched back to `Add Care` and `Add Person` inside the scoped workspace.
- Browser check of pre-workspace secondary tabs: passed. People, Care Board, Visitors, and Prayer show `Create workspace` / `Learn plan` controls plus a private-workspace gate before signup, rather than write buttons that cannot save without an account.
- Browser check of pre-workspace page headers: passed. Fresh `/app/` with no cookie/localStorage rendered People, Plan, and Integrations with `Create a workspace first.` headers, `Setup` detail toggle text, and workspace/learning CTAs instead of search, approval queue, or connector workflow language. Console warnings/errors were `0`; screenshot saved at `/tmp/marge-preworkspace-headers.png`.
- Browser check of first-run connector guidance: passed. A temporary workspace named "Browser Audit Church 20260517" saved role, size, church context, follow-up burden, and tools through the visible chat UI; the desk then surfaced Google Workspace, Planning Center, and Breeze setup cards from those saved tools. Opening Breeze setup showed the encrypted-storage requirement before any API key could be entered.
- Browser check of connected-but-unverified integration state: passed. The Planning Center card showed **Check credentials before syncing ministry data** and the empty synced-context row showed **Check Planning Center** instead of **Sync**.
- Chat credential check: passed. `scripts/smoke_connected_providers.py` asserts "Check Planning Center credentials before syncing" verifies the connector, records `verified_at`, imports no connected context, and queues no actions. `scripts/smoke_integrations.py` now covers the same first-pastor path for the suggested "Check Google Workspace credentials." prompt after OAuth callback plus the shorter "Check Google Workspace." phrase, including the guard that prevents connector commands from being saved as profile/onboarding answers. `scripts/smoke_first_run.py` separately verifies "Planning Center for kids check-in and Gmail" remains ministry tool context, not a credential-check command. It also asserts a premature "Sync Planning Center" request performs only the no-sync credential check first and asks for an explicit follow-up sync, that "Sync the connected tools" syncs verified Planning Center, Microsoft 365, and Breeze without defaulting to Google, that "What meetings need prep?" lists synced calendar events without creating extra approval actions, and that incomplete Outlook calendar-write prompts route to "What calendar details do you need?" help without creating actions.
- Visitor follow-up chat routing: passed. "Show visitors needing follow-up." now routes to `visitor_context_lookup` and recalls the saved visitor instead of being intercepted by the generic care follow-up matcher.
- Generic follow-up prioritization: passed. "Who needs follow-up?" now routes to cross-domain `prioritize_day` with pastoral priority cards instead of defaulting to care-only lookup.
- Care follow-up scheduling prompt: passed. The backend-suggested "Where can I fit care follow-up?" now routes to `care_visit_plan_queued`, chooses a real active care case, returns a calendar-block review item, and keeps external calendar writes behind approval.
- MCP sync precheck: passed. `scripts/smoke_mcp_first_run.py` asserts the MCP `sync_integration` tool turns an unchecked-provider sync rejection into a no-sync credential verification, imports no ministry context, queues no actions, and asks for an explicit follow-up sync.
- Staff role gate check: passed. `scripts/smoke_integrations.py` asserts staff tokens cannot change profile/policies, inspect or prepare approval actions, read pastoral briefing/care/prayer/member detail, generate drafts, write member/visitor records, or bypass through legacy `/chat/`.
- LLM documentation cleanup: passed. `README.md`, `mcp_server/README.md`, `BUILD_SUMMARY.md`, `docs/AI_AGENT_ROADMAP.md`, and `docs/LAUNCH_PLAN.md` now describe the 2026-05-17 state instead of the old April/Monday MVP, including the public `/` bootstrap check and the rule that MCP-only verification is not pilot readiness; `codex-task.md` is explicitly marked as historical prompt text, and `AGENTS.md` documents the current encrypted-credential setup labels plus production/live verifier `next_actions` behavior.
- SQLite `PRAGMA foreign_key_check`: `0`.
- SQLite `PRAGMA integrity_check`: `ok`.
- Smoke/debug/browser workspace residue: `0`.

## Readiness Gate Result

`scripts/verify_pilot_readiness.py` still fails locally for the right reasons:

- Missing production `DATABASE_URL`.
- `MARGE_ENV=production` is not set in the local environment.
- `MARGE_AUTO_CREATE_SCHEMA=false` is not set in the local environment.
- `MARGE_REQUIRE_ACCOUNT_TOKEN` is not set to `true` in the local environment.
- `MARGE_ENCRYPTION_KEY` is not a valid production Fernet key in the local environment.
- Secure session cookies, exact public `/app` URL, origin-only CORS, and SMTP invite/login email delivery are not configured locally.
- The local runtime reports `require_account_token=false`; production must report `true` through `/assistant/config`.
- No live external church-tool provider is verified. MCP alone does not satisfy the live-provider requirement.

The gate now prints a **Next actions** section that tells the operator to fill the deployment environment, rerun production config verification, connect a real provider, run the no-sync credential check, and rerun live connector verification with an owner/admin/pastor session token.

## Remaining Work

The goal is not complete until a real deployment provides production secrets, applies migrations, and verifies at least one live provider without syncing ministry data first. Use:

```bash
MARGE_API_URL=https://marge.yourchurch.org \
MARGE_ACCOUNT_TOKEN=REPLACE_WITH_OWNER_ADMIN_PASTOR_SESSION \
.venv/bin/python scripts/verify_pilot_readiness.py --env-file .env.production.candidate
```

Only after that passes should a pilot pastor import or sync real pastoral data.
